<Window x:Class="Sutra.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Sutra Reader" Height="600" Width="1000">
    <Window.Resources>
        <Style x:Key="TabCloseButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#F3F3F3" BorderBrush="#DDD" BorderThickness="0,0,1,0">
            <StackPanel Margin="10">
                <TextBlock Text="Sutra Sidebar" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                <Button Content="New Tab" Click="NewTab_Click" Margin="0,5" Padding="5"/>
                <TextBlock Text="Bookmarks" Margin="0,10,0,5" Foreground="Gray"/>
                <Button Content="Google" Click="SidebarBookmark_Click" Tag="https://www.google.com" Margin="0,2"/>
                <Button Content="GitHub" Click="SidebarBookmark_Click" Tag="https://www.github.com" Margin="0,2"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#EEE" BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="5">
                <StackPanel Orientation="Horizontal">
                    <Button Content="←" Width="30" Margin="2" Click="Back_Click"/>
                    <Button Content="→" Width="30" Margin="2" Click="Forward_Click"/>
                    <Button Content="↻" Width="30" Margin="2" Click="Reload_Click"/>
                    <Button Content="⌂" Width="30" Margin="2" Click="Home_Click"/>

                    <TextBox x:Name="UrlTextBox" Width="500" Margin="5,2" VerticalContentAlignment="Center" 
                 KeyDown="UrlTextBox_KeyDown"/>

                    <Button Content="Go" Width="50" Margin="2" Click="Go_Click"/>
                </StackPanel>
            </Border>

            <TabControl x:Name="BrowserTabs" Grid.Row="1">
            </TabControl>
        </Grid>
    </Grid>
</Window>
